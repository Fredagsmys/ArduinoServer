package utils;

// import java.time.LocalDate;
// import java.time.format.DateTimeFormatter;
// import java.util.List;
// import java.util.stream.Collectors;

public class Utils {
//	public static String getDatesBetween(String startDate, String endDate) {
//		String rString = "";
//		DateTimeFormatter formatter = DateTimeFormatter.ofPattern("yyyy-MM-dd");
//		LocalDate staDate = LocalDate.parse(startDate, formatter);
//		LocalDate stoDate = LocalDate.parse(endDate, formatter).plusDays(1);
//		List<LocalDate> localDateList = staDate.datesUntil(stoDate).collect(Collectors.toList());
//		for (LocalDate lDate : localDateList) {
//			rString+= (lDate.toString() + "|");
//		}
//		rString = rString.substring(0, rString.length() - 1);
//		System.out.println(rString);
//		return rString;
//		}
}
